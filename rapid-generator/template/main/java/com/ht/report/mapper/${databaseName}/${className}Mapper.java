<#assign className = table.className>   
<#assign classNameLower = className?uncap_first>   
package ${basepackage}.mapper.${databaseName};

public interface ${className}Mapper extends ${className}MapperGen{
	
}
